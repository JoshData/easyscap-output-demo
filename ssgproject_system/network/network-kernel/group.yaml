id: xccdf_org.ssgproject.content_group_network-kernel
title: Kernel Parameters Which Affect Networking
description: |
  The `sysctl` utility is used to set parameters which affect the
  operation of the Linux kernel. Kernel parameters which affect networking
  and have security implications are described here.
rules: []
subgroups:
- network_host_parameters
- network_host_and_router_parameters
