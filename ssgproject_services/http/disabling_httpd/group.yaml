id: xccdf_org.ssgproject.content_group_disabling_httpd
title: Disable Apache if Possible
description: |
  If Apache was installed and activated, but the system does not need to
  act as a web server, then it should be disabled and removed from the
  system.
rules:
- disable_httpd
- uninstall_httpd
subgroups: []
