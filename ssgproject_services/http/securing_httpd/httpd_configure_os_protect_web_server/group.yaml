id: xccdf_org.ssgproject.content_group_httpd_configure_os_protect_web_server
title: Configure Operating System to Protect Web Server
description: |
  The following configuration steps should be taken on the machine which
  hosts the web server, in order to provide as safe an environment as
  possible for the web server.
rules: []
subgroups:
- httpd_restrict_file_dir_access
- httpd_configure_iptables
- httpd_chroot
