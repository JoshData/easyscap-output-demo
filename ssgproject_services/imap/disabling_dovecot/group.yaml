id: xccdf_org.ssgproject.content_group_disabling_dovecot
title: Disable Dovecot
description: |
  If the system does not need to operate as an IMAP or POP3 server, the
  dovecot software should be disabled and removed.
rules:
- disable_dovecot
- uninstall_dovecot
subgroups: []
